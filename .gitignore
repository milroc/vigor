# deps / build
node_modules/
dist/

# runtime user data written by server.cjs
.env
backups/
store/
# labels/ holds hand-labeled ground truth: personal data (*.json) stays local,
# reproduction logic (*.cjs) and schema examples (*.example.json) are committed.
labels/*
!labels/*.example.json
!labels/*.cjs
telemetry/
targets.json

# dev artifacts (screenshots of personal health data)
mocks/

# tooling / local state
.claude/
.gstack/
.DS_Store
*.log
